@model Chocolate.DataAccess.ViewModels.PurchaseViewModel

@{
    ViewData["Title"] = "Purchases";
}

<h2>Create New Purchase</h2>
<hr />

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card-box">
                <div class="p-2">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <form asp-action="Create">

                        <div class="form-group row">
                            <label asp-for="DateReceived" class="col-md-2 col-form-label">Date Received</label>
                            <div class="col-md-10">
                                <input asp-for="DateReceived" value="" class="form-control" />
                            </div>
                            <span asp-validation-for="DateReceived" class="text-danger"></span>
                        </div>

                        <div class="form-group row">
                            <label asp-for="OfferId" class="col-md-2 col-form-label">Offer</label>
                            <div class="col-md-10">
                                <select asp-for="OfferId" class="form-control" asp-items="@(new SelectList(Model.Offers,"Id","Name"))"></select>
                            </div>
                            <span asp-validation-for="OfferId" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn btn-bordered-primary waves-effect  width-md waves-light">Submit</button>
                        <a asp-action="Index" class="btn btn-lighten-primary waves-effect waves-primary width-md">Back to List</a>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
